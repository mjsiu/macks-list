<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="<%=root_url %>">MacksList</a></li>
        <li class="active">
          <a href="<%= root_url %>">Create Listing
            <span class="sr-only">(current)</span>
          </a>
        </li>
        <li><a href="<%=root_url %>">Search</a></li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li class="dropdown">
            <a
              href="#"
              class="dropdown-toggle"
              data-toggle="dropdown"
              role="button"
              aria-haspopup="true"
              aria-expanded="false">
              <%=current_user.username%>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><a href="#">User Profile</a></li>
              <li><%= link_to "Log Out", session_url, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Sign Up", new_user_url, method: :get %></li>
          <li><%= link_to "Sign In", new_session_url, method: :get %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
